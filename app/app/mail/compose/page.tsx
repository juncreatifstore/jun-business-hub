import Link from "next/link";
import { redirect } from "next/navigation";
import { prisma } from "@/lib/prisma";
import { requireUser, can } from "@/lib/auth";
import { getAccessibleMailboxIds } from "@/lib/mail-security";
import { getMailConversation } from "@/lib/mail-thread-reader";
import { getMailComposeMeta, listMailSignatures, listMailTemplates, type MailComposeMode } from "@/lib/mail-compose-meta";
import { saveProfessionalMailDraft, updateProfessionalMailDraft } from "@/services/mail-professional-compose";
import { sendProfessionalDraft } from "@/services/mail-professional-send";
import { PageHeader } from "@/components/app/page-header";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Field, Input, Select, Textarea } from "@/components/ui/input";

export const dynamic="force-dynamic";
const MODES=["NEW","REPLY","REPLY_ALL","FORWARD"] as const;
function extractEmail(v:string){return (v.match(/[\w.+-]+@[\w.-]+\.\w+/)||[])[0]?.toLowerCase()||"";}

export default async function ProfessionalComposePage({searchParams}:{searchParams:{mailbox?:string;source?:string;mode?:string;draft?:string}}){
 const user=await requireUser();if(!can(user,"EMAIL_DRAFT"))redirect("/app/forbidden");
 const accessibleIds=await getAccessibleMailboxIds(user,true);
 const accounts=accessibleIds.length?await prisma.mailAccount.findMany({where:{id:{in:accessibleIds},OR:[{accessTokenEnc:{not:null}},{refreshTokenEnc:{not:null}}]},orderBy:{createdAt:"asc"},select:{id:true,email:true,displayName:true}}):[];
 if(!accounts.length)redirect("/app/mail?toast_error=No accessible connected mailbox");
 const draft=searchParams.draft?await prisma.mailThread.findFirst({where:{id:searchParams.draft,mailAccountId:{in:accessibleIds}},include:{client:true}}):null;
 const draftMeta=draft?await getMailComposeMeta(draft.id):null;if(searchParams.draft&&(!draft||!draft.aiDraft||!draftMeta))redirect("/app/mail?folder=DRAFTS&toast_error=Professional draft not found or mailbox access denied");
 const requested=draft?.mailAccountId||searchParams.mailbox||"";const account=accounts.find(a=>a.id===requested)||accounts[0];
 const mode=(draftMeta?.mode||(MODES.includes((searchParams.mode||"NEW").toUpperCase() as MailComposeMode)?(searchParams.mode||"NEW").toUpperCase():"NEW")) as MailComposeMode;
 const sourceId=draftMeta?.sourceThreadId||searchParams.source||null;const source=sourceId?await prisma.mailThread.findFirst({where:{id:sourceId,mailAccountId:account.id},include:{client:true}}):null;
 const conversation=source?await getMailConversation(source.mailAccountId,source.gmailThreadId).catch(()=>[]):[];const last=conversation.at(-1);const own=account.email.toLowerCase();
 let to:string[]=draftMeta?.to??[],cc:string[]=draftMeta?.cc??[],bcc:string[]=draftMeta?.bcc??[],subject=draft?.subject??"",body=draft?.aiDraft??"",sourceGmailMessageId=draftMeta?.sourceGmailMessageId??"";
 if(!draft&&source&&last){sourceGmailMessageId=last.id;if(mode==="REPLY"||mode==="REPLY_ALL"){const sender=extractEmail(last.from);if(sender&&sender!==own)to=[sender];subject=/^re:/i.test(source.subject||"")?source.subject||"":`Re: ${source.subject||"(no subject)"}`;if(mode==="REPLY_ALL"){const all=[...last.to,...last.cc].map(x=>x.toLowerCase()).filter(x=>x!==own&&x!==sender);cc=[...new Set(all)]}}else if(mode==="FORWARD"){subject=/^fwd:/i.test(source.subject||"")?source.subject||"":`Fwd: ${source.subject||"(no subject)"}`;body=`\n\n---------- Forwarded message ----------\nFrom: ${last.from}\nDate: ${last.date.toISOString()}\nSubject: ${last.subject}\nTo: ${last.to.join(", ")}\n\n${last.body||last.snippet}`}}
 const [clients,signatures,templates,files]=await Promise.all([prisma.client.findMany({orderBy:{createdAt:"desc"},take:300,select:{id:true,firstName:true,lastName:true,internalId:true,email:true}}),listMailSignatures(account.id),listMailTemplates(),can(user,"FILE_READ")?prisma.file.findMany({where:{isVault:false,archivedAt:null},orderBy:{createdAt:"desc"},take:80,select:{id:true,name:true,mimeType:true,sizeBytes:true,clientId:true,caseId:true}}):Promise.resolve([])]);
 const defaultSig=signatures.find(s=>s.isDefault);if(!draft&&defaultSig)body=`${body}${body?"\n\n":""}${defaultSig.body}`;const selectedFiles=new Set(draftMeta?.attachmentFileIds??[]);const action=draft?updateProfessionalMailDraft.bind(null,draft.id):saveProfessionalMailDraft;
 return <div className="space-y-5"><PageHeader title="Professional composer" subtitle={`${mode.replaceAll("_"," ")} · ${account.displayName||account.email}${draft?" · saved draft":""}`} actions={<div className="flex gap-2">{draft&&can(user,"EMAIL_SEND")?<form action={sendProfessionalDraft.bind(null,draft.id)}><Button variant="gold">Send professional email</Button></form>:null}<Link href={`/app/mail?mailbox=${account.id}&folder=${draft?"DRAFTS":"INBOX"}`}><Button variant="outline">Back to Mail</Button></Link></div>}/><Card><CardHeader><CardTitle>Message</CardTitle></CardHeader><CardContent><form action={action} className="space-y-4"><input type="hidden" name="mode" value={mode}/><input type="hidden" name="sourceThreadId" value={source?.id||""}/><input type="hidden" name="sourceGmailMessageId" value={sourceGmailMessageId}/><Field label="From"><Select name="mailAccountId" defaultValue={account.id} disabled={Boolean(draft)}>{accounts.map(a=><option key={a.id} value={a.id}>{a.displayName?`${a.displayName} · `:""}{a.email}</option>)}</Select>{draft?<input type="hidden" name="mailAccountId" value={account.id}/>:null}</Field><Field label="To"><Input name="to" required defaultValue={to.join(", ")} placeholder="one@example.com, two@example.com"/></Field><div className="grid gap-3 md:grid-cols-2"><Field label="CC"><Input name="cc" defaultValue={cc.join(", ")} placeholder="Optional"/></Field><Field label="BCC"><Input name="bcc" defaultValue={bcc.join(", ")} placeholder="Optional"/></Field></div><Field label="Subject"><Input name="subject" required maxLength={200} defaultValue={subject}/></Field><div className="grid gap-3 md:grid-cols-3"><Field label="Client"><Select name="clientId" defaultValue={draft?.clientId||source?.clientId||""}><option value="">— None —</option>{clients.map(c=><option key={c.id} value={c.id}>{c.firstName} {c.lastName} ({c.internalId})</option>)}</Select></Field><Field label="Template"><Select name="templateId" defaultValue={draftMeta?.templateId||""}><option value="">— None —</option>{templates.map(t=><option key={t.id} value={t.id}>{t.name}</option>)}</Select></Field><Field label="Signature"><Select name="signatureId" defaultValue={draftMeta?.signatureId||defaultSig?.id||""}><option value="">— None —</option>{signatures.map(s=><option key={s.id} value={s.id}>{s.name}{s.isDefault?" · Default":""}</option>)}</Select></Field></div><Field label="Message"><Textarea name="body" rows={16} required defaultValue={body}/></Field>{files.length?<Field label="Attach from JUN Drive (max 10)"><div className="max-h-56 overflow-auto rounded-lg border border-line p-3"><div className="grid gap-2 md:grid-cols-2">{files.map(f=><label key={f.id} className="flex items-start gap-2 rounded-md border border-line p-2 text-sm"><input type="checkbox" name="attachmentFileIds" value={f.id} defaultChecked={selectedFiles.has(f.id)} className="mt-1"/><span><span className="font-medium">{f.name}</span><span className="block text-xs text-muted2">{f.mimeType} · {Math.ceil(f.sizeBytes/1024)} KB</span></span></label>)}</div></div></Field>:null}<div className="flex flex-wrap gap-2"><Button type="submit" variant="primary">{draft?"Save changes":"Save professional draft"}</Button>{draft&&can(user,"EMAIL_SEND")?<span className="self-center text-xs text-muted2">Save changes before sending.</span>:null}</div></form></CardContent></Card>{source?<Card><CardHeader><CardTitle>Source conversation</CardTitle></CardHeader><CardContent><p className="text-sm text-muted2">{source.subject} · {conversation.length} message(s)</p></CardContent></Card>:null}</div>;
}
