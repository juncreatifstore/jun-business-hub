import { redirect } from "next/navigation";
import { requireUser } from "@/lib/auth";
import { getMonthlyCloseRevisionSummary, listMonthlyFinancialCloses } from "@/lib/company-funds-monthly-close";
import { validateFinancialMonthClose } from "@/lib/company-funds-monthly-close-validation";
import { formatDateTime, formatMoney } from "@/lib/utils";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { CalendarCheck2, LockKeyhole, RotateCcw, Landmark, FileDown, Sheet, ShieldCheck, AlertTriangle, XCircle, CheckCircle2, History } from "lucide-react";
import { closeFinancialMonthAction, reopenFinancialMonthAction } from "@/services/company-funds-monthly-close";

export const dynamic="force-dynamic";
export default async function MonthlyClosePage(){
  const user=await requireUser();if(user.role!=="SUPER_ADMIN")redirect("/app/forbidden");
  const closes=await listMonthlyFinancialCloses();const closed=closes.filter(c=>c.status==="CLOSED"),reopened=closes.filter(c=>c.status==="REOPENED");
  const summaries=await Promise.all(closes.map(c=>getMonthlyCloseRevisionSummary(c.period)));const summaryMap=new Map(summaries.map(s=>[s.current?.period||"",s]));
  const now=new Date();const suggested=new Date(Date.UTC(now.getUTCFullYear(),now.getUTCMonth()-1,1));const suggestedPeriod=`${suggested.getUTCFullYear()}-${String(suggested.getUTCMonth()+1).padStart(2,"0")}`;
  const validation=await validateFinancialMonthClose(suggestedPeriod);
  return <div className="space-y-5">
    <div><p className="text-xs uppercase tracking-[0.18em] text-muted2">Super Admin · Contrôle comptable</p><h1 className="mt-1 text-3xl font-semibold">Clôture financière mensuelle</h1><p className="mt-1 max-w-4xl text-sm text-muted2">Crée un snapshot officiel de la trésorerie et verrouille le mois contre les corrections rétroactives non autorisées. Chaque réouverture et nouvelle clôture est maintenant conservée comme une révision distincte.</p></div>
    <div className="grid gap-3 md:grid-cols-4"><Metric icon={LockKeyhole} label="Mois clôturés" value={String(closed.length)} hint="Périodes actuellement verrouillées"/><Metric icon={RotateCcw} label="Réouverts" value={String(reopened.length)} hint="Périodes rouvertes pour correction"/><Metric icon={CalendarCheck2} label="Dernière clôture" value={closes[0]?.period||"—"} hint={closes[0]?formatDateTime(closes[0].closedAt):"Aucune clôture"}/><Metric icon={History} label="Révisions" value={String(summaries.reduce((n,s)=>n+s.revisions.filter(r=>r.event==="CLOSED").length,0))} hint="Snapshots de clôture conservés"/></div>

    <Card><CardHeader><CardTitle>Contrôle préalable · {suggestedPeriod}</CardTitle></CardHeader><CardContent><div className={`mb-4 rounded-xl border p-4 ${validation.canClose?"border-emerald-200 bg-emerald-50":"border-red-200 bg-red-50"}`}><div className="flex items-center gap-2 font-semibold">{validation.canClose?<CheckCircle2 className="h-5 w-5 text-emerald-700"/>:<XCircle className="h-5 w-5 text-red-700"/>}{validation.canClose?"Période prête à être clôturée":"Clôture bloquée"}</div><div className="mt-1 text-xs">{validation.blockingCount} blocage(s) · {validation.warningCount} avertissement(s). JUN vérifie rapprochements, autorisations, preuves, transferts, synchronisations de comptes et réserves.</div></div><div className="grid gap-3 md:grid-cols-2">{validation.checks.map(check=><div key={check.code} className={`rounded-xl border p-3 ${check.severity==="BLOCKING"?"border-red-200 bg-red-50/60":check.severity==="WARNING"?"border-amber-200 bg-amber-50/60":"border-emerald-200 bg-emerald-50/40"}`}><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2 text-sm font-semibold">{check.severity==="BLOCKING"?<XCircle className="h-4 w-4 text-red-700"/>:check.severity==="WARNING"?<AlertTriangle className="h-4 w-4 text-amber-700"/>:<ShieldCheck className="h-4 w-4 text-emerald-700"/>}{check.label}</div><span className="rounded-full bg-white px-2 py-1 text-[10px] font-semibold">{check.count}</span></div><p className="mt-2 text-xs text-muted2">{check.detail}</p></div>)}</div></CardContent></Card>

    <Card><CardHeader><CardTitle>Clôturer un mois</CardTitle></CardHeader><CardContent><div className="mb-4 rounded-xl border border-amber-200 bg-amber-50 p-3 text-xs text-amber-900"><strong>Effet :</strong> le snapshot fige les comptes, réserves, prêts, investissements et flux Finance JUN du mois. Après réouverture, une nouvelle clôture crée une nouvelle révision sans écraser l&apos;ancienne.</div><form action={closeFinancialMonthAction} className="grid gap-3 md:grid-cols-[180px_1fr_auto]"><label className="text-xs">Période<input name="period" type="month" defaultValue={suggestedPeriod} required className="mt-1 w-full rounded-lg border border-line px-3 py-2"/></label><label className="text-xs">Note de clôture<input name="note" placeholder="Contrôles effectués, rapprochements, commentaires CFO..." className="mt-1 w-full rounded-lg border border-line px-3 py-2"/></label><button disabled={!validation.canClose} className="self-end rounded-lg bg-ink px-4 py-2 text-sm font-medium text-white disabled:cursor-not-allowed disabled:opacity-40">Clôturer le mois</button></form>{!validation.canClose?<p className="mt-2 text-xs font-medium text-red-700">Résolvez les blocages du contrôle préalable avant de clôturer {suggestedPeriod}.</p>:null}</CardContent></Card>
    <Card><CardHeader><CardTitle>Historique et snapshots officiels</CardTitle></CardHeader><CardContent className="space-y-4">{closes.map(c=>{const summary=summaryMap.get(c.period);const closedRevisions=summary?.revisions.filter(r=>r.event==="CLOSED")||[];return <div key={c.id} className={`rounded-xl border p-4 ${c.status==="CLOSED"?"border-emerald-200":"border-amber-200 bg-amber-50/30"}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><div className="text-lg font-semibold">{c.period}</div><span className="rounded-full bg-surface px-2 py-1 text-[10px] font-semibold">Révision {c.revision}</span></div><div className="mt-1 text-xs text-muted2">Clôturé {formatDateTime(c.closedAt)} · {c.status}</div>{c.closeNote?<div className="mt-1 text-xs">{c.closeNote}</div>:null}</div><div className="flex flex-wrap items-center gap-2"><a href={`/api/company-funds/monthly-close/${c.period}/pdf`} className="inline-flex items-center gap-1 rounded-lg border border-line bg-white px-3 py-2 text-xs font-semibold"><FileDown className="h-3.5 w-3.5"/>Rapport PDF</a><a href={`/api/company-funds/monthly-close/${c.period}/audit`} className="inline-flex items-center gap-1 rounded-lg border border-line bg-white px-3 py-2 text-xs font-semibold"><Sheet className="h-3.5 w-3.5"/>Audit CSV</a><span className={`rounded-full px-2 py-1 text-[10px] font-semibold ${c.status==="CLOSED"?"bg-emerald-50 text-emerald-700":"bg-amber-100 text-amber-800"}`}>{c.status}</span></div></div><div className="mt-4 grid gap-2 md:grid-cols-4"><Small label="Comptes" value={String(c.snapshot.accounts.length)}/><Small label="Réserves" value={String(c.snapshot.reserves.length)}/><Small label="Prêts" value={String(c.snapshot.loans.length)}/><Small label="Investissements" value={String(c.snapshot.investments.length)}/></div>{c.snapshot.financeByCurrency.length?<div className="mt-4 overflow-x-auto"><table className="w-full min-w-[760px] text-xs"><thead><tr className="border-b text-left text-muted2"><th className="p-2">Devise</th><th className="p-2 text-right">Entrées</th><th className="p-2 text-right">Remboursements</th><th className="p-2 text-right">Dépenses</th><th className="p-2 text-right">Frais</th><th className="p-2 text-right">Net</th></tr></thead><tbody>{c.snapshot.financeByCurrency.map(r=><tr key={r.currency} className="border-b border-line/60"><td className="p-2 font-semibold">{r.currency}</td><td className="p-2 text-right">{formatMoney(r.income,r.currency)}</td><td className="p-2 text-right">{formatMoney(r.refunds,r.currency)}</td><td className="p-2 text-right">{formatMoney(r.expenses,r.currency)}</td><td className="p-2 text-right">{formatMoney(r.fees,r.currency)}</td><td className={`p-2 text-right font-semibold ${r.net>=0?"text-emerald-700":"text-red-700"}`}>{formatMoney(r.net,r.currency)}</td></tr>)}</tbody></table></div>:<p className="mt-3 text-xs text-muted2">Aucun flux Finance JUN dans cette période.</p>}{closedRevisions.length>1&&summary?.variance.length?<div className="mt-4 rounded-xl border border-blue-200 bg-blue-50/40 p-3"><div className="text-xs font-semibold text-blue-900">Écart depuis la clôture précédente</div><div className="mt-2 grid gap-2 md:grid-cols-2">{summary.variance.map(v=><div key={v.currency} className="rounded-lg bg-white p-3 text-xs"><div className="font-semibold">{v.currency}</div><div className="mt-1 text-muted2">Δ Entrées {formatMoney(v.income,v.currency)} · Δ Sorties {formatMoney(v.refunds+v.expenses+v.fees,v.currency)} · <span className={v.net>=0?"text-emerald-700":"text-red-700"}>Δ Net {formatMoney(v.net,v.currency)}</span></div></div>)}</div></div>:null}<div className="mt-4 rounded-lg bg-surface p-3 text-xs"><strong>Historique :</strong> {closedRevisions.length} clôture(s) conservée(s){summary?.revisions.some(r=>r.event==="REOPENED")?` · ${summary.revisions.filter(r=>r.event==="REOPENED").length} réouverture(s) auditée(s)`:""}.</div>{c.status==="CLOSED"?<form action={reopenFinancialMonthAction.bind(null,c.period)} className="mt-4 flex flex-wrap gap-2 border-t border-line pt-3"><input name="reason" required minLength={10} placeholder="Motif détaillé de réouverture..." className="min-w-[300px] flex-1 rounded-lg border border-line px-3 py-2 text-xs"/><button className="rounded-lg border border-amber-300 bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-900">Réouvrir pour correction</button></form>:c.reopenReason?<div className="mt-4 rounded-lg border border-amber-200 bg-amber-50 p-3 text-xs"><strong>Motif de réouverture :</strong> {c.reopenReason}{c.reopenedAt?` · ${formatDateTime(c.reopenedAt)}`:""}</div>:null}</div>})}{!closes.length?<p className="py-6 text-center text-sm text-muted2">Aucune clôture mensuelle enregistrée.</p>:null}</CardContent></Card>
  </div>
}
function Metric({icon:Icon,label,value,hint}:{icon:typeof LockKeyhole;label:string;value:string;hint:string}){return <div className="rounded-xl border border-line bg-white p-4"><Icon className="mb-2 h-5 w-5 text-electric"/><div className="text-xs text-muted2">{label}</div><div className="mt-1 text-2xl font-semibold">{value}</div><div className="mt-1 text-xs text-muted2">{hint}</div></div>}
function Small({label,value}:{label:string;value:string}){return <div className="rounded-lg bg-surface p-3"><div className="text-[10px] uppercase tracking-wide text-muted2">{label}</div><div className="mt-1 font-semibold">{value}</div></div>}
