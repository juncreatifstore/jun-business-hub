{
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "regions": ["cle1"],
  "crons": [
    {
      "path": "/api/cron/mail-sync",
      "schedule": "*/5 * * * *"
    }
  ]
}
